export const StorageKeys = {
  themeMode: '@gosafe:theme_mode',
  userSession: '@gosafe:user_session',
  cachedComplaints: '@gosafe:cached_complaints',
  emergencyContacts: '@gosafe:emergency_contacts',
} as const;
